[project]
name = "vendor-bid-automation-pipeline"
version = "2.0.0"
description = "Automated vendor bid processing pipeline with ERP integration"
authors = [{name = "Max Ramos"}]
license = {text = "MIT"}
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"

[tool.black]
line-length = 99
